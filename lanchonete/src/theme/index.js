import { colors } from './colors';
import { fontSizes, fontWeights } from './typography';
import { spacing, radius } from './spacing';

export { colors, fontSizes, fontWeights, spacing, radius };

export const theme = { colors, fontSizes, fontWeights, spacing, radius };
